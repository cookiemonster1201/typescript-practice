// for avoiding using magic numbers

// by default options without values have the values of indexes 0... (Small = 0, Medium = 1 and so on)

enum pizzaSize {
  Small = "s",
  Medium = "m",
  Large = "l",
}

console.log(pizzaSize.Small);

export {};
